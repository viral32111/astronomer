{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "process",
			"label": "Build in Docker",
			"command": "${workspaceRoot}/build.sh",
			"options": {
				"cwd": "${workspaceFolder}"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			}
		}
	]
}
